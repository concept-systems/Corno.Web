@using Corno.Web.Areas.Kitchen.Dto.Put_To_Light
@using Corno.Web.Globals
@using Corno.Web.Helper
@using Kendo.Mvc.UI
@{
    Layout = "~/Views/Shared/_AppLayout.cshtml";
    ViewBag.BoxTitle = "Trolley Config";
}
@Html.ValidationSummary(false, "", new { @class = "text-danger" })
<div class="grid-container-full-height">
    @(Html.Kendo().Grid<TrolleyConfigIndexDto>()
        .Name("grid")
        .ApplyIndexSettings() // 👈 Apply index settings (full height, responsive)
        .AddExportToolBar(enableExcel: true, enablePdf: true, enableSearch: true, customToolbar: toolbar =>
        {
            toolbar.Custom()
                .Text("<span class='k-icon k-i-plus'></span> Add New")
                .HtmlAttributes(new { @class = "k-grid-add-new k-button-add-new-outlined", style = "float: right;", onclick = $"window.location.href='{Url.Action("Create", "TrolleyConfig")}'; return false;" });
        })
        .Events(events => events.ApplyCommonEvents())
        .Columns(columns =>
        {
            columns.Bound(m => m.DueDate)
                .Format("{0:dd/MM/yyyy hh:mm tt}");
            columns.Bound(m => m.LotNo).Align(TextAlign.Left);
            //columns.Bound(m => m.Family).Align(TextAlign.Left);
            columns.AddStatusColumn();
            columns.AddCommandColumns(new List<GridCommandConfig>
            {
                new GridCommandConfig { Name = "Edit", Text = "", IconClass = "fa fa-edit", ClickHandler = "onEditClick" },
                //new GridCommandConfig { Name = "Delete", Text = "", IconClass = "fa fa-trash", ClickHandler = "onDeleteClick" },
                //new GridCommandConfig { Name = "View", Text = "", IconClass = "k-icon k-i-preview k-i-eye", ClickHandler = "onViewClick" },
                new GridCommandConfig { Name = "Close", Text = "", IconClass = "fa fa-close", ClickHandler = "onCloseClick" }
            });
        })
        .DataSource(dataSource => dataSource
            .ApplyCommonDataSourceSettings("GetIndexViewDto", "TrolleyConfig", "Kitchen")
            .Sort(s => s.Add(FieldConstants.Id).Descending())
        ))
</div>

@*@(Html.Kendo().Grid<TrolleyConfigIndexDto>()
    .Name("grid")
    .ToolBar(toolbar =>
    {
        toolbar.Excel();
        toolbar.Pdf();
        toolbar.Search();
    })
    .ToolBar(t => t.Search())
    .Pageable()
    .Sortable()
    .Filterable(f => f.Mode(GridFilterMode.Row))
    .Resizable(resize => resize.Columns(true)) // Allow column resizing
    .PersistSelection()
    .HtmlAttributes(new { @class = "w-100" })
    .Columns(columns =>
    {
        columns.Select()
            .HtmlAttributes(new { style = "text-align: center" })
            .Width(10);
        columns.Bound(m => m.DueDate).Title("Date")
            .HtmlAttributes(new { style = "text-align: center" })
            .HeaderHtmlAttributes(new { @class = "k-text-center !k-justify-content-center" })
            .Format("{0:dd/MM/yyyy HH:mm tt}");
        columns.Bound(m => m.LotNo)
            .HtmlAttributes(new { style = "text-align: center" })
            .HeaderHtmlAttributes(new { @class = "k-text-center !k-justify-content-center" });
        columns.Bound(m => m.Family)
            .HtmlAttributes(new { style = "text-align: center" })
            .HeaderHtmlAttributes(new { @class = "k-text-center !k-justify-content-center" });
        columns.Bound(m => m.Status)
            .HtmlAttributes(new { style = "text-align: center" })
            .ClientTemplate(
                "<span class='badge' style='background-color: " +
                "# if (Status == \"Active\") { #" +
                "green" +
                "# } else { #" +
                "transparent" +
                "# } #" +
                "; color: " +
                "# if (Status == \"Active\") { #" +
                "white" +
                "# } else { #" +
                "black" +
                "# } #" +
                "; text-align: center; display: inline-block;'>" +
                "#= Status #</span>"
            )
            .HtmlAttributes(new { @class = "k-text-center !k-justify-content-center" })
            .HeaderHtmlAttributes(new { @class = "k-text-center !k-justify-content-center" });
        columns.Command(command =>
        {
            command.Custom("Edit").Text("")
                .Text("<i class='k-icon k-i-edit k-text-info k-icon-20'></i>")
                .HtmlAttributes(new { @class = "k-grid-edit" })
                .Click("onEditClick");
            //Show only if Status is "Active";
            command.Custom("Close").Text(" ").IconClass("k-icon k-i-close  k-text-error")
                .HtmlAttributes(new { @class = "k-grid-view k-text-center !k-justify-content-center" })
                .Click("onCloseClick");
        })
            .HeaderHtmlAttributes(new { @class = "k-text-center !k-justify-content-center" })
            .HtmlAttributes(new { @class = "k-text-center !k-justify-content-center" })
            .Title("Actions"); // Add a class to identify the command column;
    })
    .Excel(excel => excel
        .FileName("TrolleyLabel.xlsx")
        .ProxyURL(Url.Action("Excel_Export_Save", "Corno"))
    )
    .Pdf(pdf => pdf
        .FileName("TrolleyLabel.pdf")
        .ProxyURL(Url.Action("Excel_Export_Save", "Corno")))
    .DataSource(dataSource => dataSource
                 .Ajax()
                 .Read(read => read.Action("GetIndexViewModels", "TrolleyConfig"))
        .Sort(s => s.Add(FieldConstants.DueDate).Descending())
    ))*@

<script type="text/javascript">
    function onEditClick(e) {
        e.preventDefault();
        var dataItem = this.dataItem($(e.currentTarget).closest("tr"));
        window.location.href = '@Url.Action("Edit", "TrolleyConfig", new {area="Kitchen"})' + '/' + dataItem.Id;
    }

    function onCloseClick(e) {
        e.preventDefault();
        var dataItem = this.dataItem($(e.currentTarget).closest("tr"));
        window.location.href = '@Url.Action("Close", "TrolleyConfig")' + '/' + dataItem.Id;
    }

    function sendData() {
    }
</script>